function processArray(nums):
    initialize slow = 0

    for fast from 0 to length(nums) - 1:
        if nums[fast] meets condition:
            nums[slow] = nums[fast]
            slow = slow + 1

    return slow
