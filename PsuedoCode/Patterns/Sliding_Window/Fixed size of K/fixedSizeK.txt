start = 0
sum = 0
answer = -infinity   # or 0 depending on problem

for end in range(0, n):
    sum += nums[end]     # expand the window

    if end - start + 1 == k: // this is correct, while loop is not required.
        update answer (like maxSum, minSum, avg, etc.)
        sum -= nums[start]   # shrink from left
        start += 1           # slide the window forward
