“Partition an array with a small fixed number of categories (usually 3) into separate regions in one pass using three pointers.”

Even shorter mental trigger:

Few distinct values → partition with 3 pointers

Example where this applies:

Sort Colors

arrays containing only {0,1,2} or {negative, zero, positive} types.

Ultra-short memory trick

If you see:

array contains only 3 types of values
sort/group them in-place

Your brain should immediately think:

Dutch National Flag

 !----When Dutch National Flag is the Right Choice ----!

Use it when the problem says something like:

Elements belong to 3 categories

Partition into less than / equal / greater than

0, 1, 2 type problems

in-place rearrangement

O(n) time and O(1) space


You can only use this algorithm if the relative order of the element doesn't matter.


If the order is really matters, then you need to use stable partition.